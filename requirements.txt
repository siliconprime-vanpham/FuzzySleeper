# FuzzySleeper — pinned dependencies (Phase 1 + Phase 2)
# Verify these resolve in your GPU environment, then freeze with `pip freeze > requirements.lock`.
# Versions chosen for Qwen2 + TransformerLens compatibility as of mid-2025/2026.

# --- core ---
torch>=2.3.0
transformers>=4.43.0
accelerate>=0.33.0

# --- fine-tuning (plant the backdoor) — CUDA-ONLY, won't install on macOS/MPS ---
unsloth                    # 2x faster / lower-VRAM LoRA; pin the resolved version into requirements.lock
trl>=0.9.6
peft>=0.12.0
datasets>=2.20.0

# --- activation extraction (workhorse for Modules 1-3) ---
transformer-lens>=2.4.0
# fallback if transformer-lens chokes on Qwen2:
# baukit @ git+https://github.com/davidbau/baukit.git

# --- probes / analysis / plots ---
scikit-learn>=1.5.0
numpy>=1.26.0
pandas>=2.2.0
matplotlib>=3.9.0
seaborn>=0.13.0

# --- dataset seeds / utils ---
tqdm>=4.66.0

# --- cross-environment artifact sync (3070 <-> Kaggle <-> Colab via HF Hub) ---
huggingface_hub>=0.24.0
